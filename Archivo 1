function Hearth()  {
  this.x = Math.random() * ww;
  this.y = Math.random() * wh;
  this.opacity = Math.random() * 0.5 +0.5;
  this.vel = ]
    x: (math.random() -0.5) * 4,
    y: (Math.random() -0.5) * 4,
};
this.tarjetScale = Match.random() * 0.15 + 0.02;
this.scale = this.tarjetScale * Math.random();
}
